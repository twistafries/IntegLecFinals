module Hangman_Game
{
	interface Hangman_Interface
	{
		boolean login(in string username);
		string wordToGuess(in string username);
		boolean guessTheLetter(in string username, in string letter);
		string getLife(in string username);
		boolean gameOver(in string username);
		void logout(in string username);
	};
};